# Changelog

App repos pin to a tag of this repo (`@v1`, `@v2`, …). Bump major when the deploy contract changes in a backward-incompatible way.

## Unreleased

- Initial scaffold: `deploy-cloudrun-service`, `deploy-cloudrun-job`, `schedule-job`, reusable GitHub Actions workflows for service and job deploys, Workload Identity Federation docs, NAT-gateway and secret bootstrap scripts.

## v1 (target)

First tagged release after `data-workbench` and `high-ground-agents` are both deploying through this repo successfully.
